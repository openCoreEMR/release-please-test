/**
 * Test module for release-please annotated tags feature
 */

function greet(name) {
  return `Hello, ${name}!`;
}

function farewell(name) {
  return `Goodbye, ${name}!`;
}

function welcome(name) {
  return `Welcome aboard, ${name}!`;
}

module.exports = { greet, farewell, welcome };
